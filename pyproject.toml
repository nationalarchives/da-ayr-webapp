[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "da-ayr-webapp"
version = "0"
dependencies = [
    "django",
    "python-dotenv"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black"
]
